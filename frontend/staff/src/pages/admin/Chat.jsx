import StaffChat from '../../components/StaffChat.jsx';

/**
 * Чат администратора филиала.
 *
 * До этого здесь лежали 1275 строк, которые ничего никуда не отправляли:
 * список собеседников был захардкожен («Aziz Karimov», «Salom, bugun dars
 * bormi?»), сокет не подключался вовсе, а «своё/чужое» сообщение определялось
 * не по `sender_id`. Поверх этого были надстроены ответы на сообщение,
 * индикатор «печатает», удаление переписки и поиск по истории — ни одной из
 * этих операций бэкенд не умеет: в `chat.routes.js` есть только список
 * контактов, история, отправка и отметка о прочтении. Интерфейс обещал
 * функции, которых нет, поверх данных, которых нет.
 *
 * Реализация теперь общая с панелью ментора (`components/StaffChat.jsx`):
 * роли различаются только охватом, а его считает бэкенд
 * (`listStaffContacts` — у ментора свои группы, у админа весь филиал).
 */
export default function AdminChat() {
  return <StaffChat variant="admin" />;
}
